pipelines:
  - name: pipelines_S_Bash_0295
    steps:
      - name: S_Bash_0295
        type: Bash
        configuration:
          integrations:
            - name: i_generic
        execution:
          onExecute:
            - echo "executing step..."
