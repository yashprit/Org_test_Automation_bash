pipelines:
  - name: S_Bash_0057
    steps:
      - name: api_pipeline
        type: Bash
        configuration:
          outputResources:
            - name: myOutputResource
        execution:
          onExecute:
            - echo "executing step..."
            - echo "hi"