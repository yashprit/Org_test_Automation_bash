pipelines:
  - name: pipelines_S_Bash_0370
    steps:
      - name: S_Bash_0370
        type: Bash
        configuration:
          integrations:
            - name: i_slack
        execution:
          onExecute:
            - echo "executing step..."
