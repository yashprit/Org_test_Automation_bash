pipelines:
  - name: S_Bash_0014
    steps:
      - name: api_pipeline
        type: Bash
        configuration:
          affinityGroup: bldGroup 
        execution:
          onExecute:
            - echo "hi"

      - name: api_pipelineauto
        type: Bash
        configuration:
          affinityGroup: bldGroup
        execution:
          onExecute:
            - echo "hi"
