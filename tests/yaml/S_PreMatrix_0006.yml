pipelines:
  - name: PreMatrix_pipeline
    steps:
      - name: PreMatrix_step
        type: PreMatrix
        execution:
          onExecute:
            - echo "I am on PreMatrix step"
            - set_step_var Pre_Matrix value1
