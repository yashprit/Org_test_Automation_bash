pipelines:
  - name: pipelines_S_Bash_0364
    steps:
      - name: S_Bash_0364
        type: Bash
        configuration:
          integrations:
            - name: i_pemKey
        execution:
          onExecute:
            - echo "executing step..."
