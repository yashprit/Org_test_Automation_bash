pipelines:
  - name: pipelines_S_Bash_0415
    steps:
      - name: S_Bash_0415
        type: Bash
        configuration:
          integrations:
            - name: BSI_GoogleCloud
        execution:
          onExecute:
            - echo "executing step..."
