pipelines:
  - name: pipelines_S_Bash_0237
    steps:
      - name: S_Bash_0237
        type: Bash
        configuration:
          integrations:
            - name: s_artifactory
          runtime:
            type: host
        execution:
          onExecute:
            - echo "executing step..."
