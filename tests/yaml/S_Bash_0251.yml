pipelines:

  - name: pipelines_S_Bash_0251
    steps:
      - name: S_Bash_0251
        type: Bash
        configuration:
          integrations:
            - name: BSI_AzureKey
        execution:
          onExecute:
            - echo "executing step..."
