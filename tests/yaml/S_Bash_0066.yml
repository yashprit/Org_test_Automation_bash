pipelines:
  - name: S_Bash_0066
    steps:
      - name: S_Bash_0066
        type: Bash
        configuration:
          runtime:
            type: host
        execution:
          onExecute:
            - echo "executing step..."
            - export run_number=foo
