pipelines:
  - name: pipelines_S_Bash_0278
    steps:
      - name: S_Bash_0278
        type: Bash
        configuration:
          integrations:
            - name: i_dockerRegistry
        execution:
          onExecute:
            - echo "executing step..."
