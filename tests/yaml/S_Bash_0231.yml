pipelines:

  - name: pipelines_S_Bash_0231
    steps:
      - name: S_Bash_0231
        type: Bash
        configuration:
          integrations:
            - name: i_airBrake
        execution:
          onExecute:
            - echo "executing step..."
