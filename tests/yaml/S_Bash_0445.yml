pipelines:
  - name: S_Bash_0445
    steps:
      - name: S_Bash_0445
        type: Bash
        configuration:
          Runtime:
        execution:
          onExecute:
            - echo "executing step..."
