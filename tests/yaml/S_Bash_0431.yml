pipelines:
  - name: S_Bash_0431
    steps:
      - name: S_Bash_0431
        type: Bash
        configuration:
          runtime:
            type: host
        execution:
          onExecute:
            - echo "executing step..."
            - echo "hi"