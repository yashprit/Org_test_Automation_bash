pipelines:
  - name: pipelines_S_Bash_0367
    steps:
      - name: S_Bash_0367
        type: Bash
        configuration:
          integrations:
            - name: BSI_Slack
          runtime:
            type: host
        execution:
          onExecute:
            - echo "executing step..."
