pipelines:

  - name: pipelines_S_Bash_0411
    steps:
      - name: S_Bash_0411
        type: Bash
        configuration:
          integrations:
            - name: i_azureKey
        execution:
          onExecute:
            - echo "executing step..."
