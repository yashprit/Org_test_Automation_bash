pipelines:
  - name: pipelines_S_Bash_0452
    steps:
      - name: S_Bash_0452
        type: Bash
        configuration:
          integrations:
            - name: i_IncomingWebhookBasicAuth
        execution:
          onExecute:
            - echo "executing step..."
