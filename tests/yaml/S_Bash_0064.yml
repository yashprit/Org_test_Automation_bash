pipelines:
  - name: S_Bash_0064
    steps:
      - name: S_Bash_0064
        type: Bash
        execution:
          onExecute:
            - echo "onExecute"
          onComplete:
            - echo "onComplete"
