pipelines:

  - name: pipelines_S_Bash_0251
    steps:
      - name: S_Bash_0251
        type: Bash
        configuration:
          integrations:
            - name: i_azureKey
        execution:
          onExecute:
            - echo "executing step..."
