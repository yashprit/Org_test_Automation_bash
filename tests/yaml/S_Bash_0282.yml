pipelines:
  - name: pipelines_S_Bash_0282
    steps:
      - name: S_Bash_0282
        type: Bash
        configuration:
          integrations:
            - name: BSI_DockerRegistry
        execution:
          onExecute:
            - echo "executing step..."
