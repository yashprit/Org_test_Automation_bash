pipelines:

  - name: pipelines_S_Bash_0413
    steps:
      - name: S_Bash_0413
        type: Bash
        configuration:
          integrations:
            - name: BSI_FileServer
        execution:
          onExecute:
            - echo "executing step..."
