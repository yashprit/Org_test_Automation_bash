resources:

  - name: S_Bash_0439_FileSpec
    type: FileSpec
    configuration:
      sourceArtifactory: s_artifactory
      pattern: "example-repo-local/setup/"
      recursive: false
      sortBy: invalid

pipelines:

  - name: pipelines_S_Bash_0439
    steps:
      - name: S_Bash_0439
        type: Bash
        configuration:
          inputResources:
            - name: S_Bash_0439_FileSpec
        execution:
          onExecute:
            - echo "executing step..."
