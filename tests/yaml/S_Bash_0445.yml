pipelines:
  - name: pipeline_automation
    steps:
      - name: api_pipeline
        type: Bash
        configuration:
          Runtime:
        execution:
          onExecute:
            - echo "executing step..."
