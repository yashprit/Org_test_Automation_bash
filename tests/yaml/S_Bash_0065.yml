pipelines:
  - name: S_Bash_0065
    steps:
      - name: api_pipeline
        type: Bash
        configuration:
          integrations:
            - name: my_integrations
        execution:
          onExecute:
            - echo "executing step..."
            - echo "hi"