pipelines:
  - name: S_Bash_0432
    steps:
      - name: S_Bash_0432
        type: imag
        configuration:
          runtime:
            type: host
        execution:
          onExecute:
            - echo "executing step..."
            - echo "hi"
