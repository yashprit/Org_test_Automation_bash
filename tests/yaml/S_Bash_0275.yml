pipelines:
  - name: pipelines_S_Bash_0275
    steps:
      - name: S_Bash_0275
        type: Bash
        configuration:
          integrations:
            - name: i_dockerRegistry
        execution:
          onExecute:
            - echo "executing step..."
