pipelines:

  - name: pipelines_S_Bash_0243
    steps:
      - name: S_Bash_0243
        type: Bash
        configuration:
          integrations:
            - name: BSI_AWS
          runtime:
            type: host
        execution:
          onExecute:
            - echo "executing step..."
