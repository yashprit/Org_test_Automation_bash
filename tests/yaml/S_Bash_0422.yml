pipelines:
  - name: S_Bash_0422
    steps:
      - name:
        type: Bash
        configuration:
          runtime:
            type: host
        execution:
          onExecute:
            - echo "executing step..."
