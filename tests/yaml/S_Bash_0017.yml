pipelines:
  - name: S_Bash_0017
    steps:
      - name: api_pipeline
        type: Bash
        configuration:
          nodePool: byon
          affinityGroup: bldGroup@$ 
        execution:
          onExecute:
            - echo "hi"

      - name: api_pipelineauto
        type: Bash
        configuration:
          nodePool: byon
          affinityGroup: bldGroup@$
          inputSteps:
            - name: api_pipeline
        execution:
          onExecute:
            - echo "hi"
