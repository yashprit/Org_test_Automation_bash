pipelines:

  - name: pipelines_S_Bash_0473
    steps:
      - name: S_Bash_0473
        type: Bash
        configuration:
          integrations:
            - name: s_githubEnterprise
        execution:
          onExecute:
            - echo "executing step..."
