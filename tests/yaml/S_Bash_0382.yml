pipelines:
  - name: pipelines_S_Bash_0382
    steps:
      - name: S_Bash_0382
        type: Bash
        configuration:
          integrations:
            - name: BSI_SSHKEY
        execution:
          onExecute:
            - echo "executing step..."
