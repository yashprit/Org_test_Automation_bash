pipelines:
  - name: pipeline_automation
    steps:
      - name: api_pipeline
        type: Bash
        configuration:
          integrations:
            - name: s_gitHub
        execution:
          onExecute:
            - echo "executing step..."
