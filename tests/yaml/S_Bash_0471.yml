pipelines:

  - name: pipelines_S_Bash_0471
    steps:
      - name: S_Bash_0471
        type: Bash
        configuration:
          integrations:
            - name: BSI_AirBrake
        execution:
          onExecute:
            - echo "executing step..."
