pipelines:

  - name: pipelines_S_Bash_0261
    steps:
      - name: S_Bash_0261
        type: Bash
        configuration:
          integrations:
            - name: s_bitbucketServer
        execution:
          onExecute:
            - echo "executing step..."
