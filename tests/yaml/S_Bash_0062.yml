pipelines:
  - name: S_Bash_0062
    steps:
      - name: S_Bash_0062
        type: Bash
        configuration:
          runtime:
            type: host
        execution:
          onExecute:
            - echo "onExecute"
          onSuccess:
            - echo "onSuccess"
