pipelines:
  - name: pipelines_S_Bash_0373
    steps:
      - name: S_Bash_0373
        type: Bash
        configuration:
          integrations:
            - name: BSI_SMTP
        execution:
          onExecute:
            - echo "executing step..."
