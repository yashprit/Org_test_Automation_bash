pipelines:
  - name: pipeline_automation
    steps:
      - name: api_pipeline
        type: Bash
        configuration:
          timeoutSeconds: -5000
        execution:
          onExecute:
            - echo "hi"
