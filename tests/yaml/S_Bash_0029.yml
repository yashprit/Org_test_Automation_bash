pipelines:
  - name: S_Bash_0029
    steps:
      - name: S_Bash_0029
        type: Bash
        configuration:
          integrations:
            - name: s_gitHub
        execution:
          onExecute:
            - echo "executing step..."
