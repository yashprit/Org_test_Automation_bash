pipelines:
  - name: S_Bash_0445
    steps:
      - name: api_pipeline
        type: Bash
        configuration:
          Runtime:
        execution:
          onExecute:
            - echo "executing step..."
