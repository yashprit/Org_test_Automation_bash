pipelines:
  - name: pipelines_S_Bash_0317
    steps:
      - name: S_Bash_0317
        type: Bash
        configuration:
          integrations:
            - name: BSI_GoogleCloud
        execution:
          onExecute:
            - echo "executing step..."
