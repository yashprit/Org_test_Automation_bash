pipelines:
  - name: S_Bash_0025
    steps:
      - name: api_pipeline
        type: Bash
        configuration:
          timeoutSeconds: 2.45739
        execution:
          onExecute:
            - echo "hi"
