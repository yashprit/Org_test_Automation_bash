pipelines:
  - name: S_Bash_0030
    steps:
      - name: S_Bash_0030
        type: Bash
        execution:
          onExecute:
            - echo "executing step..."

      - name: S_Bash_0030_1
        type: Bash
        configuration:
          inputSteps:
            - name: S_Bash_0030
        execution:
          onExecute:
            - echo "executing step..."
