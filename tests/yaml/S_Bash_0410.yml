pipelines:
  - name: pipelines_S_Bash_0410
    steps:
      - name: S_Bash_0410
        type: Bash
        configuration:
          integrations:
            - name: s_artifactory
        execution:
          onExecute:
            - echo "executing step..."
