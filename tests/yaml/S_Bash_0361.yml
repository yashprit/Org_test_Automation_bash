pipelines:
  - name: pipelines_S_Bash_0361
    steps:
      - name: S_Bash_0361
        type: Bash
        configuration:
          integrations:
            - name: i_pemKey
        execution:
          onExecute:
            - echo "executing step..."
