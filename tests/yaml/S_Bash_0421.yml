pipelines:
  - name: S_Bash_0421
    steps:
      - name: S_Bash_0421$@
        type: Bash
        configuration:
          runtime:
            type: host
        execution:
          onExecute:
            - echo "executing step..."
            - echo "hi"
