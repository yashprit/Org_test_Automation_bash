pipelines:
  - name: S_Bash_0061
    steps:
      - name: S_Bash_0061
        type: Bash
        configuration:
          runtime:
            type: host
        execution:
          onExecute:
            - java --version
          onFailure:
            - echo "Job well done!"
