pipelines:
  - name: pipelines_S_Bash_0333
    steps:
      - name: S_Bash_0333
        type: Bash
        configuration:
          integrations:
            - name: BSI_Jenkins
        execution:
          onExecute:
            - echo "executing step..."
