pipelines:
  - name: pipelines_S_Bash_0333
    steps:
      - name: S_Bash_0333
        type: Bash
        configuration:
          integrations:
            - name: i_jenkins
        execution:
          onExecute:
            - echo "executing step..."
