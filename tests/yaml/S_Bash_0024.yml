pipelines:
  - name: S_Bash_0024
    steps:
      - name: api_pipeline
        type: Bash
        configuration:
          timeoutSeconds: -5000
        execution:
          onExecute:
            - echo "hi"
