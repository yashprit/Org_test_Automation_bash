pipelines:
  - name: S_Bash_0014
    steps:
      - name: S_Bash_0014
        type: Bash
        configuration:
          runtime:
            type: host
          affinityGroup: bldGroup 
        execution:
          onExecute:
            - echo "hi"

      - name: S_Bash_0014_1
        type: Bash
        configuration:
          runtime:
            type: host
          affinityGroup: bldGroup
        execution:
          onExecute:
            - echo "hi"
