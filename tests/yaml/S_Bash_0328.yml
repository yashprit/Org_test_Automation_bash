pipelines:
  - name: pipelines_S_Bash_0328
    steps:
      - name: S_Bash_0328
        type: Bash
        configuration:
          integrations:
            - name: BSI_Jira
        execution:
          onExecute:
            - echo "executing step..."
