pipelines:
  - name: pipelines_S_Bash_0346
    steps:
      - name: S_Bash_0346
        type: Bash
        configuration:
          integrations:
            - name: BSI_Jenkins
        execution:
          onExecute:
            - echo "executing step..."
