pipelines:
  - name: pipelines_S_Bash_0361
    steps:
      - name: S_Bash_0361
        type: Bash
        configuration:
          integrations:
            - name: BSI_Pemkey
        execution:
          onExecute:
            - echo "executing step..."
