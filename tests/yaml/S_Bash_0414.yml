pipelines:
  - name: pipelines_S_Bash_0414
    steps:
      - name: S_Bash_0414
        type: Bash
        configuration:
          integrations:
            - name: s_gitHub
          runtime:
            type: host
        execution:
          onExecute:
            - echo "executing step..."
