pipelines:
  - name: S_Bash_0036
    steps:
      - name: S_Bash_0036
        type: Bash
        configuration:
          runtime:
            type: host
            image:
              custom:
                name: drydock/u16nodall
                tag: master 
        execution:
          onExecute:
            - echo "executing step..."
            - echo "Hi"
