pipelines:
  - name: pipeline_automation
    steps:
      - name: S_Bash_0011
        type: Bash
        configuration:
          nodePool: u16dynamic
        execution:
          onExecute:
            - echo "executing step..."
            - |
              [ $step_node_pool_name == "u16dynamic"  ]
