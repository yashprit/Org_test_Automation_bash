pipelines:

  - name: pipelines_S_Bash_0290
    steps:
      - name: S_Bash_0290
        type: Bash
        configuration:
          integrations:
            - name: BSI_FileServer
        execution:
          onExecute:
            - echo "executing step..."
