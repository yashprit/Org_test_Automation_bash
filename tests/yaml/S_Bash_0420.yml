pipelines:
  - name: pipelines_S_Bash_0420
    steps:
      - name: S_Bash_0420
        type: Bash
        configuration:
          integrations:
            - name: i_jira
        execution:
          onExecute:
            - echo "executing step..."
