pipelines:
  - name: pipelines_S_Bash_0317
    steps:
      - name: S_Bash_0317
        type: Bash
        configuration:
          integrations:
            - name: BSI_GoogleCloud
          runtime:
            type: host
        execution:
          onExecute:
            - echo "executing step..."
