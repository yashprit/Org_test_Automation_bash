pipelines:

  - name: pipelines_S_Bash_0266
    steps:
      - name: S_Bash_0266
        type: Bash
        configuration:
          integrations:
            - name: BSI_DigitalOcean
        execution:
          onExecute:
            - echo "executing step..."
