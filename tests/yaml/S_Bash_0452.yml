pipelines:
  - name: pipelines_S_Bash_0452
    steps:
      - name: S_Bash_0452
        type: Bash
        configuration:
          integrations:
            - name: BSI_IW_BasicAuth
        execution:
          onExecute:
            - echo "executing step..."
