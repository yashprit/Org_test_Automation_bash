pipelines:
  - name: pipelines_S_Bash_0376
    steps:
      - name: S_Bash_0376
        type: Bash
        configuration:
          integrations:
            - name: BSI_SMTP
        execution:
          onExecute:
            - echo "executing step..."
